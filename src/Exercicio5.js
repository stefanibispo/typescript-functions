"use strict";
// Escreva uma função que encontre a área e o perímetro de um círculo, de acordo com o raio fornecido.
const areaPerimetroCirculo = (raio) => 2 * Math.PI * raio;
console.log(areaPerimetroCirculo(5));
